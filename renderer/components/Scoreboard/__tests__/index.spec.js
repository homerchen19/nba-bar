import { BoxScore, LineScore, PlayByPlay, Stats, TeamScore } from '../index';

it('be defined', () => {
  expect(BoxScore).toBeDefined();
  expect(LineScore).toBeDefined();
  expect(PlayByPlay).toBeDefined();
  expect(Stats).toBeDefined();
  expect(TeamScore).toBeDefined();
});
